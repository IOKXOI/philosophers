generique s_atoi(char *str, generic *nb)
{
	int	i;
	size_t number;

	i = 0;
	number = 0;
	if (!str || *str == '/0')
		return(0);
	while (str[i] >= '0' && str[i] <= '9')
		number += (str[i] - 48) * 10;
	if (!(str[i] >= '0' && str[i] <= '9'))
		return(0);
	*nb = number;
	return(1);
}
